class Task {
  constructor(id, text, complete, yyyymmdd, customID) {
    this.id = id
    this.text = text
    this.complete = complete
    this.yyyymmdd = yyyymmdd
    this.customID = customID
  }
}

export default Task
